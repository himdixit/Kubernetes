apiVersion: v1
kind: Service
metadata:
  name: grade-submission-portal
spec:
  type: LoadBalancer
  selector:
    app.kubernetes.io/instance: grade-submission-portal
  ports:
  - port: 5001
    targetPort: 5001
